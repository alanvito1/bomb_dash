# This file is an example. Copy it to .env and fill in the values for your local environment.
# These variables are used by oracle.js and server.js

# The private key of the account to be used by the oracle.
# For local Hardhat node, you can get this from the node's startup logs.
# IMPORTANT: DO NOT COMMIT THE .env FILE WITH A REAL PRIVATE KEY.
ORACLE_PRIVATE_KEY=

# The JSON-RPC URL of the blockchain network.
# For a local Hardhat node, this is the default.
BSC_RPC_URL=http://127.0.0.1:8545/

# The addresses of the deployed smart contracts.
# These will be automatically populated in backend/contracts/contract-addresses.json
# after running the deployment script, but are listed here for completeness.
# The oracle service will read them from the JSON file, not from here.
TOURNAMENT_CONTROLLER_ADDRESS=
PERPETUAL_REWARD_POOL_ADDRESS=

# A secret key used to sign JSON Web Tokens (JWT).
# Use a long, random string for production.
JWT_SECRET=your-default-secret-key-for-testing

# The port the backend server will run on.
PORT=3000