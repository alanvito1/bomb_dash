# .env.test configuration
NODE_ENV=test
PORT=3001

# Segurança (Use valores falsos, mas com formato válido)
# Chave privada de teste (não use uma real)
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
JWT_SECRET=test_secret_key_12345
ADMIN_SECRET=admin_test_secret

# Configuração de Domínio (CRÍTICO para corrigir o erro SIWE Domain Mismatch)
# O erro "Blocked login attempt from invalid domain" ocorre porque o teste envia 'localhost'
# mas o backend espera 'undefined' ou outro valor. Vamos alinhar:
FRONTEND_URL=http://localhost:3000
FRONTEND_DOMAIN=localhost
SIWE_DOMAIN=localhost

# Banco de Dados (Use memória ou SQLite local para testes rápidos)
# Se estiver usando Sequelize com SQLite em memória para testes:
DATABASE_URL=sqlite::memory:

# Blockchain (Mock para não precisar de RPC real)
# O Hardhat local geralmente roda na porta 8545
TESTNET_RPC_URL=http://localhost:8545
CHAIN_ID=1
